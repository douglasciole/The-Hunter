using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Networking;

public class HTTP : MonoBehaviour {
    
    /*IEnumerator Upload()
    {
        /*WWWForm form = new WWWForm();
        form.AddField("chave", "wef1wef65w1ef65w1ef84");
        form.AddField("nome", "Fulerinho");
        form.AddField("pontos", "300000");


        UnityWebRequest www = UnityWebRequest.Post("http://arqueiro.gustavoandrade.net/store", form);
        yield return www.SendWebRequest();

        if (www.isNetworkError || www.isHttpError)
        {
            Debug.Log(www.error);
        }
        else
        {
            Debug.Log("Form upload complete!");
        }
    }*/

}
